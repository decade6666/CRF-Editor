# Spec: R3 — 项目复制按钮常显

## Scope
前端：项目列表中的复制按钮展示。
后端：不变。

## Functional Requirements
- 每个项目项的复制按钮持续可见，不依赖 hover 才出现。
- 仅调整显示逻辑，不改变现有复制接口、命名规则、owner 重绑或深拷贝语义。
- 复制中的 loading 状态只影响当前项目项。

## Acceptance Criteria
- [ ] 项目列表中每个项目都常显复制按钮
- [ ] 鼠标未悬停时复制按钮仍可见
- [ ] 点击复制后的行为与现有后端返回一致
