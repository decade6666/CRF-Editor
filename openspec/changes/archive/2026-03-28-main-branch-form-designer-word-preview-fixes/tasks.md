## 1. 预览规则与字段覆盖值语义收敛

- [x] 1.1 收敛 `FormDesignerTab.vue` 中默认值输入的展示条件：非表格场景仅对普通文本类字段显示单行覆盖值入口，同时保持表格 / inline 场景的现有默认值入口与行为不变
- [x] 1.2 调整前端 HTML 预览对 `default_value` 的应用规则：表格 / inline 场景继续对所有字段类型生效，非表格场景仅在支持字段范围内生效，且选项类字段继续按字典/占位语义渲染
- [x] 1.3 调整 `useCRFRenderer.js` 中选项类渲染分支，修复 `trailing_underscore` 的预览对齐且不改变选项文本与顺序
- [x] 1.4 对齐前端预览与后端导出在 choice 占位符、尾部下划线存在性上的共享语义

## 2. 设计器预览布局与备注展示增强

- [x] 2.1 在表单设计界面继续使用 HTML 模拟预览，并按需求放大预览容器、压缩正文宽度
- [x] 2.2 在设计器预览中将 `design_notes` 展示为右侧备注区域，且空备注时不渲染空占位块
- [x] 2.3 校验设计器内预览与现有主预览复用同一渲染规则，避免形成双份预览语义

## 3. 数据联动与字段更新一致性

- [x] 3.1 修复快速编辑字典名称后的前端刷新链路，确保属性面板、当前编辑态与预览都读取最新 codelist 名称
- [x] 3.2 修复字段单位清空流程，保存时显式提交 `unit_id: null` 并以回读结果刷新本地状态
- [x] 3.3 对齐 `inline_mark` 与 `default_value` 的状态迁移契约，移除旧的 clear-on-disable 假设，并补充与当前实现一致的回归验证

## 4. 导入与导出链路一致性补齐

- [x] 4.1 补齐模板导入链路对 `CodeListOption.trailing_underscore` 的保真复制
- [x] 4.2 校验导入后的字典选项在后续预览与导出中保持与源模板一致的尾线语义
- [x] 4.3 为字段覆盖值边界、单位清空、字典联动刷新与 trailing underscore 保真补充自动化验证检查点