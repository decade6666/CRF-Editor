## 1. 前端预览与设计器交互收敛

- [x] 1.1 调整 `useCRFRenderer.js` 中选项类渲染分支，修复 `trailing_underscore` 的预览对齐且不改变选项文本与顺序
- [x] 1.2 在表单设计弹窗内复用现有 Word 预览布局与渲染数据，展示当前表单的实时预览
- [x] 1.3 在前端 Word 预览中展示 `design_notes`，并处理空备注时不渲染空占位块
- [x] 1.4 收敛 `FormDesignerTab.vue` 中默认值输入与预览应用规则，仅在支持的非表格字段类型上开放并保持重复渲染稳定
- [x] 1.5 补齐四种 choice 字段类型（`单选`、`多选`、`单选（纵向）`、`多选（纵向）`）的统一字典必填校验，防止 `多选（纵向）` 漏检

## 2. 设计器数据联动与字段保存一致性

- [x] 2.1 修复快速编辑字典名称后的前端刷新链路，确保属性面板与预览都读取最新 codelist 名称
- [x] 2.2 修复字段单位清空流程，保存时显式提交 `unit_id: null` 并以回读结果刷新本地状态
- [x] 2.3 为上述交互补充可验证检查点，覆盖幂等性、回读一致性与支持/非支持字段边界

## 3. 模板导入字典复用与尾线语义保真

- [x] 3.1 在模板导入中引入同名字典语义签名比较，只有当顺序、`code`、`decode`、`trailing_underscore` 全部一致时才复用目标字典
- [x] 3.2 当同名字典语义冲突时，按 `名称（导入）`、`名称（导入2）` 规则新建导入字典，而不是静默复用
- [x] 3.3 补齐模板预览与模板导入执行对结构化 option metadata（至少 `code`、`decode`、`trailing_underscore`）的共享契约
- [x] 3.4 为模板导入补充自动化验证，覆盖同名但 `trailing_underscore` 不同、`code` 不同、顺序不同三类冲突场景

## 4. DOCX 导入与导出对 choice 语义的闭环修复

- [x] 4.1 以 `field_type` 为权威，补齐 `多选（纵向）` 在 DOCX 导入、持久化与重新加载中的稳定语义
- [x] 4.2 修复导出链路中 `多选（纵向）` 的纵向布局行为，避免仅放开入口判断而实际退化为横向多选
- [x] 4.3 明确并实现 DOCX 选项末尾下划线按字面文本处理的规则，禁止仅凭尾部 `_` 自动合成 `trailing_underscore` metadata
- [x] 4.4 为 DOCX 导入与导出补充回归验证，覆盖 `多选（纵向）` round-trip、字面下划线文本保留、以及导出布局稳定性

## 5. 集成验证与不变式检查

- [x] 5.1 为模板导入 → 保存 → 导出链路补充集成测试，验证同名字典复用不会改变来源导出语义
- [x] 5.2 为 `多选（纵向）` 补充 preview/save/reload/export 的 round-trip 测试，确保字段类型和布局语义保持稳定
- [x] 5.3 为 `unit_id: null`、字典重命名刷新、默认值支持边界补充自动化验证，确保重复操作保持幂等
- [x] 5.4 在验证中明确区分两条不变式：模板 DB 的 `trailing_underscore` 必须保真；DOCX 中字面 `_` 必须保持字面文本而不是被隐式转码