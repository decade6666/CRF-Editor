# Tasks: admin-auth-sort-fixes

## Phase 0: 统计口径与项目排序作用域收口

- [x] 0.1 修改 `backend/src/services/user_admin_service.py` 的 `list_users()`，将 `project_count` 调整为只统计 `deleted_at IS NULL` 的活跃项目
- [x] 0.2 修改 `backend/src/services/user_admin_service.py` 的 `delete_user()`，删除前校验只统计活跃项目，确保与用户列表口径一致
- [x] 0.3 补后端测试：用户只剩回收站项目时，`GET /api/admin/users` 显示 `project_count == 0`，且 `DELETE /api/admin/users/{id}` 允许成功
- [x] 0.4 核对并修正项目 reorder 的后端作用域定义，确保只校验当前活跃项目，不把软删除项目纳入完整 ID 列表
- [x] 0.5 补项目排序测试：存在回收站项目时，活跃项目拖拽后仍可成功持久化，刷新后顺序不回弹且序号连续

## Phase 1: 设置壳层与退出登录

- [x] 1.1 修改 `frontend/src/App.vue` 的设置打开逻辑：所有登录用户都可打开设置弹窗，但仅管理员才请求 `/api/settings`
- [x] 1.2 在设置弹窗中拆出用户轻量设置区，确保普通用户不会再看到“加载设置失败: 需要管理员权限”
- [x] 1.3 在设置弹窗增加“退出登录”按钮，并封装前端退出流程：清 token、重置登录态、返回登录入口
- [x] 1.4 保留上次用户名输入值，确保退出后登录页自动回填或继续显示最近用户名
- [x] 1.5 补前端/集成验证：普通用户打开设置不报错；管理员仍能读取与保存全局设置；退出登录后返回登录入口且用户名保留

## Phase 2: 用户聚合数据库导出

- [x] 2.1 在 `backend/src/routers/export.py` / `backend/src/services/export_service.py` 新增“当前用户全部项目聚合数据库导出”接口与服务逻辑
- [x] 2.2 明确聚合导出的数据范围：仅包含当前登录用户拥有的项目及其关联资源，不包含其他用户项目
- [x] 2.3 保持 `GET /api/export/database` 仅管理员可用，不放开整库备份权限
- [x] 2.4 修改 `frontend/src/App.vue` 的“导出数据库”按钮逻辑：普通用户调用聚合导出接口，管理员仍可使用整库导出能力
- [x] 2.5 保留“导出当前项目”能力，不改变单项目导出入口与行为
- [x] 2.6 补后端测试：普通用户导出结果只含自己的项目；管理员整库导出边界不变；单项目导出继续可用

## Phase 3: 字典左侧拖拽排序

- [x] 3.1 修改 `frontend/src/components/CodelistsTab.vue`，为左侧字典列表接入拖拽排序交互
- [x] 3.2 复用现有 `POST /api/projects/{project_id}/codelists/reorder` 接口，不新增新的 codelist 排序协议
- [x] 3.3 保持手工改序号与拖拽排序使用同一持久化真值，避免刷新回弹
- [x] 3.4 补验证：字典左侧拖拽后立即持久化，刷新后顺序保持一致，`order_index` 连续无跳号

## Phase 4: 访视内表单批量排序

- [x] 4.1 在 `backend/src/routers/visits.py` 新增 VisitForm 批量 reorder 端点，例如 `POST /api/visits/{visit_id}/forms/reorder`
- [x] 4.2 在 `backend/src/services/order_service.py` 复用现有 `sequence` 批量重排能力，对 `VisitForm.sequence` 实现完整作用域校验与连续序号落库
- [x] 4.3 修改 `frontend/src/components/VisitsTab.vue`，为右侧访视内表单列表接入拖拽排序，drop 后一次性提交完整列表
- [x] 4.4 保留手工改 `sequence` 能力，但确保它与拖拽排序共享同一后端真值
- [x] 4.5 补测试：访视内表单拖拽后立即持久化、刷新不回弹、`sequence` 连续为 `1..n`

## Phase 5: 全局拖拽排序一致性回归

- [x] 5.1 盘点项目、字典、字典选项、字段、表单、访视、访视内表单等所有可拖拽区域，确认 drop 后都走“完整作用域列表”协议
- [x] 5.2 为现有拖拽列表补或更新测试，验证保存后序号连续且无跳号
- [x] 5.3 核对过滤/搜索态的拖拽禁用逻辑，避免提交局部列表导致排序失败
- [x] 5.4 回归验证刷新、重新进入页面、导出/预览后顺序保持一致

## Phase 6: 导入重名行为定位与修复

- [x] 6.1 复现实测“项目导出后再导入若重名会失败”的具体路径，确认问题发生在导出文件内容、导入校验链路还是前端提示路径
- [x] 6.2 保留 `backend/src/services/project_import_service.py` 的 `_resolve_import_name()` 规则，不重写现有 `(导入N)` 命名策略
- [x] 6.3 若真实失败点在前端提示或接口返回，修正成功提示为最终项目名，并展示 rename 后名称
- [x] 6.4 补测试：同名项目导入不会因名称冲突失败，导入结果中的最终项目名与实际创建项目一致

## Phase 7: 主界面蓝色主题微调

- [x] 7.1 盘点主界面蓝色主色来源，优先定位全局主题变量 / 调色板，而不是组件内硬编码颜色
- [x] 7.2 统一将主蓝色系微调为更浅、更柔和的版本
- [x] 7.3 检查浅色与暗色模式下的按钮、选中态、边框、提示文本对比度，避免可读性退化
- [x] 7.4 补最小视觉回归验证，确保这是主题微调而不是大规模样式重构

## Phase 8: 综合回归验证

- [x] 8.1 执行管理员用户管理相关后端测试，确认 `project_count` 与用户删除行为已按活跃项目口径对齐
- [x] 8.2 执行设置、退出登录、数据库导出相关前后端验证，确认普通用户与管理员权限边界正确
- [x] 8.3 执行排序相关回归，确认项目、字典、访视、访视内表单等区域顺序持久化且连续
- [x] 8.4 执行导入重名回归，确认重名项目导入成功并显示最终名称
- [x] 8.5 人工验证主界面蓝色微调在浅色/暗色模式下都保持可读性
