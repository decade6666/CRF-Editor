# CRF 元数据管理系统依赖
# 使用 ~= 兼容版本约束，允许 patch 升级（安全更新），阻止 breaking 的 minor/major 升级
# 部署前请运行 pip-compile 生成 requirements.lock 以精确锁定版本

# Web Framework
fastapi~=0.110.0
uvicorn[standard]~=0.27.0

# Database
SQLAlchemy~=2.0.0

# Config
pyyaml~=6.0.0

# File Upload
python-multipart~=0.0.9

# Word Export
python-docx~=1.0.0

# HTTP Client (AI API调用)
httpx~=0.27.0
