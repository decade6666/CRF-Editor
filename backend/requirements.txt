# CRF Metadata Management System Dependencies
# Use ~= for compatible version constraints (allows patch upgrades, blocks minor/major breaking changes)
# Run pip-compile to generate requirements.lock for precise version pinning before deployment

# Web Framework
fastapi~=0.110.0
uvicorn[standard]~=0.27.0

# Database
SQLAlchemy~=2.0.0

# Config
pyyaml~=6.0.0

# File Upload
python-multipart~=0.0.9

# Auth
PyJWT>=2.8.0

# Word Export
python-docx~=1.0.0

# HTTP Client (AI API calls)
httpx~=0.27.0
